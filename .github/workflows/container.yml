name: deploy-container

on:
  push:
    branches:
      - master
    paths:
      - 'packages/container/**'

defaults:
  run:
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://mfe-dashboard-kubson/container/latest
        env:
          AWS_ACCESS_KEY_ID: AKIA45SUPK364CV6H7MK
          AWS_SECRET_ACCESS_KEY: 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
          AWS_DEFAULT_REGION: "us-east-2"